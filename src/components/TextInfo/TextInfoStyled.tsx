import { Typography } from '@mui/material';
import styled from '@emotion/styled';

export const TextInfoStyled = styled(Typography)`
  font-family: 'Nunito', sans-serif;
  font-size: 25px;
  color: #ffff;
  @media (max-width: 576px) {
    font-size: 22px;
  }
`;
